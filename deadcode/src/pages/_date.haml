.field
  = label(id, label)
  %br
  %input(name=id id=id)
  :javascript
    load_handlers.push(function (info) {
      var val = info['#{id}'] || '';
      $('##{id}').get(0).value = val;
    });
    save_handlers.push(function (info) {
      info['#{id}'] = $('##{id}').get(0).value;
    });